{
  "name": "@keymesh/types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared primitive types and utilities for the KeyMesh monorepo.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@keymesh/config": "*",
    "@types/node": "^22.10.2",
    "typescript": "5.9.2",
    "vitest": "^2.1.3"
  }
}
